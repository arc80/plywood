config_list {
    config "Debug" {
        platform.with_asserts = true
    }
    config "RelWithAsserts" {        
        platform.with_asserts = true
        compile_options {
            optimization = "size"
        }
    }
    config "Release" {
        compile_options {
            optimization = "size"
        }
    }
}
